# 项目现状与后续计划

## 截至 2026/8/30 14:12 的项目现状

当前项目已经具备一套完整、可运行的多轮商品搜索流程。Agent 能理解用户逐步补充的要求、维护对话状态，并结合精确匹配、BM25 和可选的语义搜索，从 50,000 件商品中返回 Top 10 推荐。

目前离线方案在 200 个公开测试案例中通过了 199 个，Hit Rate@10 为 0.995，唯一失败的是 `public_0020`。加入 Qwen Dense Retrieval 后，主要改善了部分目标商品在推荐列表中的位置，但整体提升较小。现有方案仍然以 lexical search 为主，语义搜索只起辅助作用。

系统当前的优势是稳定、可复现，而且即使远程模型不可用，也能回退到离线检索。主要不足是策略比较固定：buying 和 browsing 基本走同一条流程，用户画像利用很少，追问几乎只使用 `other`，LLM 除了 Embedding 和可选的 Query Rewrite/Rerank 外，没有真正参与意图理解、策略选择或失败恢复。

接下来的重点不应该只是让更多环节使用 LLM，而是让每项新能力都解决一个明确问题，同时避免破坏目前已经通过的 199 个案例。

## 1. 唯一失败案例：public_0020

### 目前识别到的问题

`public_0020` 是当前唯一没有在 10 轮内找到目标商品的案例。目标是一件带有 Grandma 文案的长袖 novelty T-shirt，同时也可以被理解为女士服装、家庭礼物或母亲节礼物。它的身份信息比较模糊，评分数量也很少，很容易被大量相似商品压到后面。

目前还不能直接断定这是语义搜索、排序或提问策略的问题。它可能是目标从未进入候选范围，也可能是进入候选后排名不够高，或者用户在整个对话中没有提供足够的信息来区分它。

### 我们 propose 的解决方案

先把这个案例作为完整的 failure study，确认问题属于候选召回、最终排序还是信息不足。只有完成归因后，再决定应该优化检索、调整排序、改善追问，还是加强候选轮换。

修复时不能只关注它是否成功，还要确认原来通过的案例不会因此退步，避免为了一个公开案例造成整体过拟合。

### 我们的发现

复盘后确认，目标商品一直在候选榜单中，但最终只排在约第 214 名，因此这不是召回失败，而是排序和信息不足共同造成的问题。

公开 evaluator 只会从预先生成的 attribute 中回答问题。对于这个案例，Agent 最终只能获得 cotton、grey、通用材质说明和 Imported。这些信息同时存在于大量相似商品中，而真正能区分目标的 Grandma、文字图案、Mother's Day gift 和 long sleeve 等信息不会被 evaluator 披露。

因此，单纯增加 LLM、延长上下文或继续询问 attribute，都无法凭空恢复缺失的信息。Public evaluator 更像 bare-minimum test，但我们仍希望系统能够兼容更自然、信息更丰富的真实用户或 private conversation。

### 我们最终的解决方案

采用兼容两种场景的双轨策略。

Agent 首先用自然语言询问更有区分度的信息，例如文字或图案、送礼对象、使用场合和具体款式，同时继续输出 evaluator 能理解的结构化 `ask_attribute`。真实用户可以补充 Grandma、Mother's Day gift 等关键信息；低版本 evaluator 仍然可以按照 attribute contract 正常回复。

如果用户或 evaluator 无法提供更多信息，Agent 就判断信息已经耗尽，不再重复无效追问，而是进入探索模式。探索模式应优先考虑完整 attribute 组合，降低通用 popularity signal 对低评论商品的压制，并从不同商品 identity 或 use-case 的候选簇中选择不重复的结果，最大化 10 轮内的有效覆盖率。

这套方案不能在缺乏信息时保证唯一识别目标，但它不会针对 `public_0020` 写死规则，同时能够兼容 bare-minimum evaluator 和更自然的未来对话。

#### Clarification Policy：如何决定下一步问什么

Intent recognition、user profile 和 ask-attribute 不应被当成三个完全独立的功能。它们需要共同组成一个统一的 Clarification Policy。

每一轮，Agent 先判断用户是在明确购买还是仍在浏览，再排除用户已经回答或拒绝过的问题。User profile 用来提示用户通常重视哪些维度，当前候选商品之间的主要差异则决定什么问题最有区分度。最终问题由这三部分共同决定：用户还没有说什么、什么信息最能区分当前候选，以及用户通常最关心什么。

系统应先决定“问什么”，再生成自然的表达，同时输出 evaluator 能理解的 `ask_attribute`。LLM 可以帮助把问题说得自然，但不应脱离当前候选和对话状态随意决定策略。

#### `public_0020` 的理想完整流程

用户最初只说明需要 Novelty Women clothing，并要求 cotton。Agent 已经知道 material，因此不应重复询问材质。当前候选的主要区别是文字或图案、送礼对象、使用场合和具体款式，所以 Agent 可以自然地询问：是否有偏好的文字、图案、送礼对象或场合，同时使用 `other` 作为兼容 evaluator 的结构化字段。

如果面对自然用户，用户可能补充 Grandma、long sleeve 或 Mother's Day gift。Agent 获得这些高区分度信息后，就能把目标商品排到前面。

如果面对 bare-minimum evaluator，它仍然只会返回 grey、通用材质说明和 Imported。Agent 应识别到这些信息仍不足以区分目标，并在可用 attribute 耗尽后停止重复追问，转入多样化探索：优先完整匹配已知属性，同时从不同文字设计、使用场合、款式和商品 identity 的候选簇中选择不重复的结果。

因此，系统形成两个向后兼容的分支：自然用户通过开放但有针对性的问题提供更多语义信息；简单 evaluator 无法提供更多信息时，Agent 自动切换为最大化候选覆盖率的探索策略。后一个分支只能提高命中概率，不能在缺乏证据时保证唯一识别 `public_0020`。

---

## 2. User intent recognition：Buying 与 Browsing

### 目前识别到的问题

系统目前能够从特定句式识别 browsing，但识别结果没有真正改变后续策略。Buying 和 browsing 仍然使用基本相同的提问、检索和排序流程。

Buying 用户通常有较明确的购买目标，需要重视硬性要求和准确匹配；browsing 用户仍在探索，更需要多样化推荐和方向性的引导。两类用户如果完全使用相同策略，体验和检索效果都不理想。

### 我们 propose 的解决方案

为 buying 和 browsing 建立不同的 high-level 策略。Buying 模式强调明确约束、准确匹配和快速收敛；browsing 模式强调探索、推荐多样性，以及帮助用户逐渐明确需求。

LLM 可以作为不明确场景下的意图判断工具，但不应该取代所有简单、可靠的规则。重点不是增加一个标签，而是让这个标签真正影响后续行为。

---

## 3. User profile 蒸馏与使用

### 目前识别到的问题

用户画像并非完全没有使用，但目前只以很低的权重参与 lexical ranking，而且没有进入 Dense Query。

更重要的是，现有画像通常只说明用户重视哪些维度，例如 material、fit 或 comfort，并没有说明用户具体喜欢 cotton、宽松版型或某个品牌。直接把这些抽象标签当成商品偏好，可能会产生错误的排序信号。

### 我们 propose 的解决方案

将用户画像整理成两类信息：用户重视的决策维度，以及有明确证据支持的具体偏好。

“重视 material”这类信息应该主要用于决定下一步问什么；“偏好 cotton”这类明确偏好才适合进入搜索和排序。当前对话中用户明确表达的要求始终应该高于历史画像，避免画像覆盖本次真实意图。

---

## 4. Context 与 Memory Management

### 目前识别到的问题

当前对话最多只有 10 轮，而且系统主要使用结构化状态，而不是持续把完整对话发送给 LLM。因此目前还没有明显的上下文长度问题。

不过，随着后续引入更多 LLM 能力，历史消息、用户约束、否定信息和意图覆盖可能逐渐变得难以管理。简单截断旧消息可能会丢失重要要求，也可能重新引入已经被用户否定的偏好。

### 我们 propose 的解决方案

继续以结构化状态作为长期记忆的核心，只保留少量近期原始对话作为补充。无论如何压缩上下文，都应优先保存当前有效约束、用户明确否定的内容、已经问过的问题，以及被新要求替代的旧偏好。

这项工作应在 LLM 真正开始使用更多对话历史后再推进，目前不是最高优先级。

---

## 5. Prompt Self-Evolution 与失败感知

### 目前识别到的问题

LLM 目前没有真正参与策略判断，Agent 也无法识别自己是否正在重复无效推荐、是否没有获得新信息，或者检索结果是否已经停滞。

但在线运行时，Agent 并不知道 ground truth，因此它不能真正确定自己“推荐错了”。如果让模型自由修改 prompt 或策略，系统会变得难以复现，也容易发生不可控的行为漂移。

### 我们 propose 的解决方案

让 Agent 感知可观察的失败信号，而不是猜测 ground truth。例如用户持续不满意、推荐结果高度重复、候选范围没有缩小、约束互相冲突，或多种检索方式给出严重不同的结果。

发现这些信号后，Agent 应从有限、明确的恢复策略中选择下一步，例如重新确认需求、改变提问方向、放宽软性偏好或增加候选多样性。Prompt 的演进更适合在线下基于失败记录进行，并在完整回归测试后才接受新版本。

---

## 6. Ask Attribute 不能只问 `other`

### 目前识别到的问题

当前系统几乎每一轮都询问 `other`。这种做法在公开 evaluator 中非常有效，因为它能一次获得尚未披露的多种要求，但从真实对话体验来看过于笼统，也没有利用已有画像和候选商品信息。

如果直接全面改成询问具体属性，又可能降低公开评测成绩，因为具体问题只能获得对应类型的信息，有时用户并没有该方面的偏好。

### 我们 propose 的解决方案

采用混合追问策略。优先根据用户画像、当前意图和候选商品之间最有区分度的属性来提出具体问题；当无法判断最有价值的问题，或者具体问题没有获得有效信息时，再使用 `other` 作为 fallback。

Buying 用户应优先确认影响购买决策的硬性要求；browsing 用户则更适合从 style、use case 或更细的品类方向开始探索。

---

## 7. Semantic Search 与各部分权重

### 目前识别到的问题

当前融合中 lexical search 占据绝对主导地位，Dense Retrieval 主要用于小幅调整排名。现有公开结果显示 Dense Retrieval 确实带来了一点 MRR 提升，但没有提高最终命中数量。

用户画像的排序权重也非常低。不过，简单提高 Dense 或 profile 权重并不一定更好：精确品牌、材质、尺寸和预算通常更适合 lexical/exact match，而抽象风格、使用场景和同义表达更适合 semantic search。

### 我们 propose 的解决方案

从固定权重逐步转向根据 Query 类型和检索信号调整权重。明确、具体的购买约束继续以 exact 和 lexical 为主；模糊的 browsing、style 和 use-case 查询可以适当提高 semantic search 的作用。

用户画像应作为低于当前明确意图的软信号，并区分“关注某个维度”和“明确喜欢某个值”。所有权重调整都应通过完整案例集比较，而不是只根据单个失败案例或表面上的权重比例判断。

---


## 当前阶段（16:03）判断与 Next Moves

### 目前已经做到哪里

目前已经完成 Clarification Policy 的基础骨架。系统能够结合 buying/browsing intent、user profile 中关注的维度、当前候选商品之间的差异，以及之前问过的问题，决定下一轮更应该询问哪个方向。系统也能通过模板把这个方向转换成相对自然的问题，并在 evaluator 表示没有更多信息时停止重复追问。

Intent recognition 也已经改成混合方案：明确表达优先使用规则，不明确的首轮表达可以选择使用 LLM，并返回意图和置信度。不过，intent 目前主要只影响提问优先级，还没有真正改变检索、排序或推荐多样性。

当前 25 个自动化测试全部通过，离线评测仍然是 199/200，Hit Rate@10 为 0.995，唯一失败仍然是 `public_0020`。这说明现有改动保住了原来的能力，但完整的双轨系统还没有实现。

当前最明显的断点是：Agent 已经可以用自然语言询问更具体的问题，却还不能理解真实用户用自然语言给出的回答。固定 evaluator 的句式能够被 Regex 解析，但用户如果回答“我想要 Grandma 的文字、长袖、适合母亲节送礼”，这些信息目前不会进入结构化状态、Query 或检索。

### 1. 补齐自然语言回答理解

这是下一步的最高优先级。

现有固定句式继续由可靠的 Regex 处理；当 Regex 无法得到有效信息时，再使用 LLM 将自然回答整理成结构化结果，包括 category、具体约束、否定信息、偏好覆盖关系和置信度。无论信息来自 evaluator 还是自然用户，最后都应进入同一个 Session State 和 Query 流程。

完成这一项之后，Agent 才真正形成闭环：不仅知道应该问什么，也能理解用户的回答，并用新信息改变推荐结果。

### 2. 完成 Ask Attribute 的双轨协议

目前自然语言问题已经可以有明确焦点，但结构化 `ask_attribute` 仍然固定使用 `other`。下一步需要把一次提问明确拆成三个部分：系统想了解的 focus、展示给用户的自然语言 message，以及交给 evaluator 的 `ask_attribute`。

当某个具体属性有足够区分度，而且 evaluator 确实能够回答时，可以选择具体属性；当无法确定最有价值的问题、具体属性不可回答，或者具体提问没有得到有效信息时，继续使用 `other` 作为 fallback。

LLM 可以负责让问题表达得更自然，但“问什么”仍然应该由 Clarification Policy 决定，而不是交给 LLM 自由发挥。

### 3. 实现 `public_0020` 的信息耗尽探索分支

自然语言回答理解能够帮助真实用户和更复杂的 private conversation，但 bare-minimum evaluator 仍然无法提供 Grandma、long sleeve 或 Mother's Day gift 等关键信息。因此还需要处理没有更多信息时的第二条分支。

目前的 unseen rotation 只是沿着同一个基础排名跳过已经展示过的商品。目标位于约第 214 名时，即使连续轮换也可能无法在十轮内覆盖到。下一步应让系统从不同的商品 identity、style 和 use case 候选簇中选择不重复结果，而不是只展示同一排名的下一批商品。

同时可以单独实验降低 popularity signal 对低评论商品的压制，但不能直接删除或大幅修改权重。任何调整都必须通过完整公开案例集验证，避免为了 `public_0020` 造成新的 regression。

### 4. 让 Buying 与 Browsing 真正影响后续行为

Intent classifier 已经存在，接下来要让分类结果真正产生行为差异。

Buying 应更重视硬性约束、Exact/BM25 和快速收敛；Browsing 应更重视 style、use case、Dense Retrieval 和候选多样性；意图不明确时则继续使用保守策略。

这一步的重点不是继续优化意图标签本身，而是让意图影响提问方向、检索路线、融合权重和探索强度。

### 5. 蒸馏和规范化 User Profile

User profile 应明确分成两类：用户重视的决策维度，以及有直接证据支持的具体偏好。

Material、fit 这类“关注维度”主要用于决定下一步问什么；cotton、loose fit 这类“具体偏好”才适合进入 Query、Dense Retrieval 和排序。当前对话中明确表达的要求始终高于历史画像。

完成新的 profile 表示后，再重新评估目前 profile tags 直接以低权重参与 lexical ranking 的做法是否应该保留。

### 6. 重新评估 Semantic Search 与动态融合

在自然回答、intent routing 和 user profile 都稳定后，再考虑动态调整 Exact、BM25、Dense identity、Dense attribute 和 profile signal 的作用。

明确具体的 buying query 继续以 exact 和 lexical 为主；抽象的 browsing、style 和 use-case query 可以提高 Dense 和多样化探索的作用。不能仅仅因为当前权重看起来是 50:1:1，就直接提高 Dense 权重。调整前应先记录每条 retrieval route 对候选排名的实际贡献。

现有 RRF 继续负责融合不同排名列表；可选 semantic reranker 是另一条候选重排路线，两者并不需要用 LLM 重复替代。

### 7. 最后完善 Memory、Failure Recovery 与 Prompt Evolution

当前最多只有十轮，而且核心信息已经保存在结构化状态中，因此 dynamic memory 不是当前最高优先级。等自然语言解析、LLM 提问表达或其他模块开始使用更长对话历史后，再加入近期原始对话、结构化长期记忆和压缩历史的组合。

在系统主要链路稳定后，再增加重复结果、候选停滞、约束冲突、用户持续不满意和检索路线严重分歧等失败信号。发现问题后，只允许从有限、明确的恢复动作中选择下一步。

Prompt evolution 放在最后，并且只在线下根据失败记录进行。任何新 Prompt 都必须通过完整回归测试后才能替换当前版本。

## 更新后的建议实施顺序

1. 建立每一步都必须通过的回归门槛，保留当前 199/200 基线。
2. 实现自然语言回答理解，让真实用户提供的信息能够进入 State 和 Query。
3. 完成 focus、message、`ask_attribute` 和 `other` fallback 的双轨提问协议。
4. 实现信息耗尽后的商品簇探索，重点验证 `public_0020`，同时防止其他案例退步。
5. 让 buying、browsing 和 uncertain 真正影响检索与推荐策略。
6. 蒸馏 user profile，区分关注维度和具体偏好。
7. 基于 Query 类型和检索信号重新评估动态融合权重。
8. 当 LLM 开始使用更多历史后，再完善 context 和 memory management。
9. 最后增加受约束的 failure recovery，并考虑离线 prompt evolution。

当前阶段不应优先做的事情包括：为了使用 LLM 而使用 LLM、仅根据 50:1:1 直接提高 Dense 权重、在无法理解用户回答之前优先做 LLM 问句润色，以及让在线 Agent 自由修改自己的 Prompt。

每一步都应单独评测，并比较总体指标、不同 scenario 的表现、`public_0020` 的变化、API 成本和延迟，以及是否引入新的失败案例。实施前系统已经达到 199/200，因此“保持已有能力”与“增加新能力”同样重要。

## Next Moves 1–5 实施结果（2026/8/30）

本轮已经完成 Next Moves 中的前五项，并保持 official evaluator 不变。

### 1. 自然语言回答理解：已实施

回答理解现在采用三层 fallback：优先处理 evaluator 的固定句式；固定句式无法处理时，使用保守的自然语言规则；仍然不明确时，可以选择使用 LLM 输出结构化 JSON。提取结果统一进入现有 Session State，因此真实用户补充的 Grandma、long sleeve 或 Mother's Day gift 等信息能够进入后续 Query 和检索。

LLM 回答提取默认关闭。只有配置 API key 并启用 `TECHJAM_LLM_ANSWER=1` 时才调用；远程调用失败或置信度不足时会安全回退，不影响离线流程。

### 2. Ask Attribute 双轨协议：已实施

系统现在明确区分 question focus、自然语言 message 和结构化 `ask_attribute`。检测到固定 evaluator 协议时继续使用 `other`，保持 public test 的信息披露效率；自然对话中，如果问题焦点有足够置信度，则可以输出 material、style、feature 或 use_case 等具体属性。具体属性不可靠或已经多次询问时，仍然回退到 `other`。

### 3. `public_0020` 探索分支：已实施

信息耗尽后，系统不再只是沿同一榜单每次向后翻十个商品。新的探索器会从 long sleeve、recipient、gift、occasion、fit 和 outdoor 等通用候选簇中各选择未展示商品，并用不同排名深度补齐剩余位置。

在新的完整离线评测中，`public_0020` 在第 7 轮以第 1 名进入 Top 10。该实现没有写死 ASIN 或 Grandma 商品，只使用通用商品 facet。

### 4. Buying 与 Browsing 行为差异：已实施基础路由

两种意图已经使用不同的 Clarification Policy。启用 Dense Retrieval 时，Buying 继续保持 lexical 精确检索优先；Browsing 会提高 Dense identity、Dense attribute 和语义多样性的相对作用；意图不明确时使用保守权重。当前仍未对纯 lexical 内部的所有 route 做动态调权，这部分留在 Next Move 6 中统一评估。

### 5. User Profile 蒸馏：已实施

Profile 现在拆分为重要决策维度、明确正向偏好和明确负向偏好。Material、fit 等抽象标签只指导提问；只有 `explicit_preferences`、`positive_preferences` 或 `preferred_values` 中有直接证据的具体值才进入 Query 和排序。当前对话中的 active constraints 仍然拥有更高优先级。

### 回归结果

新增能力后，自动化测试从 25 个增加到 34 个并全部通过。确定性离线评测结果如下：

- Hit Rate@10：0.995 → 1.000
- MRR：0.741480 → 0.739940
- MTTC：2.055 → 2.035
- Recommended Technical Score：0.898844 → 0.901282
- 唯一失败案例 `public_0020`：未命中 → 第 7 轮 Rank 1

新的结果修复了唯一 miss，并改善了总体完成速度和技术分；MRR 有 0.00154 的小幅下降，需要在后续动态融合实验中继续观察。下一阶段应从 Next Move 6 开始：记录各 retrieval route 的贡献，再评估 lexical、dense 和 profile 的动态融合。Memory、failure recovery 和 prompt evolution 仍然保持后置。
